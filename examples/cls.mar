class Player {
    int health
    int score

    void init(int h) {
        health = h
        score = 0
    }

    void damage(int amount) {
        health = health - amount
    }
}

int main() {
    Player p = new Player(100)
    p.damage(20)
    print("Health: %d\n", p.health)
    return 0
}